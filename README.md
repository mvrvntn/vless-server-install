# Установка сервера Xray (VLESS TCP)

## Описание

Скрипт `install_xray.sh` автоматически разворачивает Xray-сервер на порту 443 TCP с использованием TLS (Let's Encrypt), который включает:

- **VLESS TCP XTLS-Vision** (Порт 443 TCP) — скоростной и стабильный стандартный протокол.
- **Поддержка подписок (Base64)**: Сервер автоматически разворачивает локальный Python-демон подписок. Ссылка вида `https://домен/sub/UUID` возвращает VLESS TCP конфигурацию для импорта во все популярные клиенты.
- **Интеграция Cloudflare WARP**: Возможность обхода геоблокировок через сеть WARP. Доступно два режима:
  - **Избирательный обход (Smart)**: через WARP направляются только заблокированные и геоблокированные ресурсы (напр. ChatGPT, Claude, Instagram, Netflix), на остальные ресурсы сохраняется прямой выход через IP сервера с минимальным пингом.
  - **Весь трафик (Full)**: весь исходящий трафик сервера оборачивается в WARP.
- **Авто-обновление списков геоблока**: Ежедневное автоматическое обновление списка заблокированных доменов с помощью cron-задачи.
- **Интеграция RoscomVPN**: Демон подписок автоматически отдаёт заголовки с актуальным роутингом (из репозитория `roscomvpn-routing`) для клиентов Happ и Incy.
- **Автоопределение геолокации**: Скрипт сам запрашивает гео-IP сервера и автоматически добавляет флаг-эмодзи страны в имя ссылки (например, 🇳🇱).
- **Кастомные имена устройств**: Возможность задать понятное имя для каждого профиля при установке (например, `iPhone_Alex`, `Macbook_Pro`).
- **Автоматизация**: Получение и автопродление Let's Encrypt сертификатов через Certbot.
- **Фаервол (UFW)**: Автоматическая настройка безопасных правил для TCP портов.

---

## Установка и запуск
#### Примечание: Скрипт использует ufw для открытия портов 443 TCP, 80 TCP и 22 TCP.
Перед установкой убедитесь в доступности порта 22, чтобы не потерять доступ к серверу:
```bash
ufw allow 22/tcp
ufw enable
```

#### Удаленное выполнение:
```bash
curl -fsSL "https://raw.githubusercontent.com/mvrvntn/vless-server-install/main/install_xray.sh?v=$(date +%s)" -o /tmp/install_xray.sh && sudo bash /tmp/install_xray.sh
```

#### Локальное выполнение:
```bash
git clone https://github.com/mvrvntn/vless-server-install/ && cd vless-server-install && sudo bash install_xray.sh
```

---

## Режимы запуска

### 1. Интерактивный режим
```bash
sudo bash install_xray.sh
```
Скрипт запросит:
- Доменное имя
- Email (для Let's Encrypt)
- Количество устройств
- Кастомное имя для каждого устройства (например, `iphone`, `home-pc`)

После завершения установки используйте:
```bash
generate_client_config
```
Выберите устройство, и утилита выведет ссылку подключения VLESS Vision с соответствующими эмодзи страны и кастомным именем, а также предложит отобразить QR-код.

### 2. Headless режим (без взаимодействия)
```bash
sudo bash install_xray.sh --headless your.domain.com your@email.com 3 "iphone" "macbook" "pc"
```
Параметры:
1. Домен
2. Email
3. Количество клиентов
### 3. Режим ручного обновления списков геоблока
```bash
sudo bash install_xray.sh --update-geoblocks
```
Позволяет вручную обновить файл `/etc/xray/geoblock.lst` с сервера GitHub, перегенерировать конфигурационный файл Xray и перезапустить службу. (По умолчанию этот процесс выполняется автоматически раз в сутки в 3:30 ночи).

---

## Удаление

Если Xray уже установлен, просто запустите скрипт повторно:
```bash
sudo bash install_xray.sh
```
Скрипт определит предыдущую установку и предложит меню, в котором можно выбрать пункт **Удалить Xray**. При этом:
- Удаляются службы и конфигурации Xray
- Удаляются cron-задачи
- Из брандмауэра удаляются правила для портов 443 (TCP) и 80 (TCP) (остальные правила UFW не затрагиваются)

---

## Выходные файлы:

- **Конфигурация сервера**: `/usr/local/etc/xray/config.json`
- **Клиентские конфиги**: `/etc/xray/client_configs/*.json`
- **Генератор ссылок**: `/usr/local/bin/generate_client_config`
- **Сервер подписок (Python)**: `/usr/local/bin/xray_sub_server.py`
- **Служба подписок (Systemd)**: `/etc/systemd/system/xray-sub.service`
- **Логи**: `/var/log/xray/`
